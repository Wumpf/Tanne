// Need to refresh 3 times:

// First without change.

// Second run:
Math.backgroundColor = "rgba(0,0,0,0)";
drawStrokedTree(new Pos(80,-20));

// Third run:
Math.backgroundColor = "rgba(0,0,0,0)";
drawStrokedTree(new Pos(0,0));